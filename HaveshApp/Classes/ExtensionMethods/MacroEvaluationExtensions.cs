using NCalc;
using PanoramicData.NCalcExtensions;
using PanoramicData.NCalcExtensions.Exceptions;

namespace HaveshApp.Classes.ExtensionMethods
{
	public static class MacroEvaluationExtensions
	{
		/*public static string? Eval(this string macro)
		{
			var options = ScriptOptions.Default
				.WithReferences(AppDomain.CurrentDomain.GetAssemblies())
				.WithGlobals(new Globals { MyVariable = 42 });

			var state = await CSharpScript.RunAsync("result = MyVariable + 1", options);
			var expression = new Expression(macro);
			
			expression.EvaluateFunction += NCalcExtensions
			return expression.Evaluate().ToString();
		}*/
	}
}
